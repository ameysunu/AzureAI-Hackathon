@inject AuthenticationStateProvider AuthStateProvider

<div class="navbar">
    Pacify
    <AuthorizeView>
        <Authorized>
            <div class="login-navbar">
                <a href="MicrosoftIdentity/Account/SignOut" style="text-decoration: none; color: red">Log out</a>
            </div>
        </Authorized>
    </AuthorizeView>
</div>

@code {
    // private string loggedInUserName = "Guest";

    // protected override async Task OnInitializedAsync()
    // {
    //     var authState = await AuthStateProvider.GetAuthenticationStateAsync();

    //     if (authState.User.Identity.IsAuthenticated)
    //     {
    //         loggedInUserName = authState.User.Claims.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/givenname")?.Value;
    //     }
    // }

}
